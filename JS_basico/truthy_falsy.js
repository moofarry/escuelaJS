// False
Boolean();
Boolean(0);
Boolean(null);
Boolean(NaN);
Boolean(undefined);
Boolean("");

//True
Boolean(1);
Boolean("a"); // con un caracter es verdadero
Boolean([]);
Boolean({});
Boolean(true);